<section class="section" id="services">
  <div class="container">
    <div class="section__header">
      <h2 class="section__title">What I Build</h2>
      <p class="section__subtitle">
        Ten years of Divi craft, distilled into three core offerings. Every project gets the same standard: fast, responsive, and SEO-ready.
      </p>
    </div>

    <div class="services__layout">
      <div class="service-card service-card--featured">
        <h3 class="service-card__title">Custom Divi Builds</h3>
        <p class="service-card__text">Full website design in Divi 5 — from blank canvas to launch. Pixel-perfect layouts, custom sections, and a visual builder structure you can manage yourself.</p>
        <ul class="service-card__list">
          <li>Bespoke design tailored to your brand</li>
          <li>Mobile-responsive on every device</li>
          <li>Built on Divi 5 for speed and future-proofing</li>
          <li>SEO structure baked in from day one</li>
        </ul>
      </div>

      <div class="service-card">
        <h3 class="service-card__title">Divi Optimization & Fixes</h3>
        <p class="service-card__text">Got an existing Divi site that needs attention? Speed optimization, layout fixes, mobile responsiveness, or structural cleanup — I diagnose and deliver.</p>
      </div>

      <div class="service-card">
        <h3 class="service-card__title">AI Design → WordPress</h3>
        <p class="service-card__text">You built a prototype in Claude, Lovable, or Bolt. I rebuild it in Divi 5 as a production WordPress site — clean, fast, and editable by anyone.</p>
        <div class="service-card__tools">
          <span>Claude</span>
          <span>Lovable</span>
          <span>Replit</span>
          <span>Bolt</span>
        </div>
      </div>
    </div>

    <div class="services__cta">
      <a href="#contact" class="btn btn--primary btn--lg">Request a Quote</a>
    </div>
  </div>
</section>

<style>
  .services__layout {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    gap: var(--space-6);
    align-items: start;
  }

  .services__layout .service-card--featured {
    grid-row: span 2;
  }

  .service-card {
    background-color: var(--color-surface-raised);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    padding: var(--space-6);
    transition: border-color var(--transition-fast);
  }

  .service-card--featured {
    background-color: var(--color-primary-surface);
    border-color: var(--color-primary-light);
    padding: var(--space-8);
  }

  .service-card:hover {
    border-color: var(--color-primary-light);
  }

  .service-card__title {
    font-size: var(--text-xl);
    margin-bottom: var(--space-3);
  }

  .service-card__text {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.7;
    font-family: var(--font-body);
    margin-bottom: var(--space-4);
  }

  .service-card__list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .service-card__list li {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    font-family: var(--font-body);
    padding-left: var(--space-5);
    position: relative;
    line-height: 1.6;
  }

  .service-card__list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.55em;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background-color: var(--color-primary);
  }

  .service-card__tools {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
    margin-top: var(--space-3);
  }

  .service-card__tools span {
    font-size: var(--text-xs);
    font-weight: 500;
    font-family: var(--font-body);
    color: var(--color-primary-dark);
    background-color: var(--color-primary-light);
    padding: var(--space-1) var(--space-3);
    border-radius: 999px;
  }

  .services__cta {
    text-align: center;
    margin-top: var(--space-10);
  }

  @media (max-width: 768px) {
    .services__layout {
      grid-template-columns: 1fr;
    }

    .services__layout .service-card--featured {
      grid-row: auto;
    }
  }
</style>
