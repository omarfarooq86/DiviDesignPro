<section class="hero" id="hero">
  <div class="container hero__inner">
    <div class="hero__content">
      <div class="hero__badge">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        Top Rated WordPress Developer
      </div>
      <h1 class="hero__title">
        Your <span class="hero__highlight">Divi Freelancer</span> for Stunning WordPress Sites
      </h1>
      <p class="hero__subtitle">
        With over a decade of experience, I craft pixel-perfect Divi websites that are fast,
        responsive, and built for conversions. Divi 5 specialist for modern,
        high-performing designs.
      </p>
      <div class="hero__actions">
        <a href="#contact" class="btn btn--primary btn--lg">Start Your Project</a>
        <a href="#portfolio" class="btn btn--outline btn--lg">View Portfolio</a>
      </div>
      <div class="hero__proof">
        <div class="hero__proof-item">
          <span class="hero__proof-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          </span>
          <span>10+ years specializing exclusively in Divi</span>
        </div>
        <div class="hero__proof-item">
          <span class="hero__proof-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          </span>
          <span>Clients include Snoop Dogg, Oxford Scholastica, and 100+ more</span>
        </div>
        <div class="hero__proof-item">
          <span class="hero__proof-icon">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
          </span>
          <span>AI design to WordPress conversion — Divi 5 specialist</span>
        </div>
      </div>
    </div>
    <div class="hero__visual" aria-hidden="true">
      <div class="hero-card">
        <div class="hero-card__top">
          <div class="hero-card__avatar">MO</div>
          <div class="hero-card__profile">
            <div class="hero-card__name">Muhammad Omar Farooq</div>
            <div class="hero-card__role">Divi Freelancer & WordPress Developer</div>
            <div class="hero-card__stars" title="5-star rated">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="#E6A817"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="#E6A817"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="#E6A817"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="#E6A817"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <svg width="16" height="16" viewBox="0 0 24 24" fill="#E6A817"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              <span>5.0</span>
            </div>
          </div>
        </div>

        <div class="hero-card__skills">
          <span>WordPress</span>
          <span>Divi 5</span>
          <span>SEO</span>
          <span>Figma</span>
        </div>

        <div class="hero-card__footer">
          <div class="hero-card__available">
            <span class="hero-card__dot"></span>
            Available for new projects
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: calc(var(--header-height) + var(--space-20));
    padding-bottom: var(--space-24);
    background: linear-gradient(175deg, var(--color-primary-light) 0%, var(--color-surface) 60%);
    overflow: hidden;
    position: relative;
    isolation: isolate;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-16);
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background-color: var(--color-primary-surface);
    color: var(--color-primary-dark);
    font-size: var(--text-xs);
    font-weight: 600;
    font-family: var(--font-body);
    padding: var(--space-2) var(--space-4);
    border-radius: 999px;
    margin-bottom: var(--space-6);
    border: 1px solid var(--color-primary-light);
  }

  .hero__badge svg {
    color: var(--color-primary);
  }

  .hero__title {
    font-size: var(--text-6xl);
    font-weight: 700;
    line-height: 1.08;
    margin-bottom: var(--space-6);
    letter-spacing: -0.02em;
  }

  .hero__highlight {
    color: var(--color-primary);
    position: relative;
  }

  .hero__highlight::after {
    content: '';
    position: absolute;
    bottom: 2px;
    left: 0;
    right: 0;
    height: 4px;
    background-color: var(--color-primary-light);
    border-radius: 2px;
    z-index: -1;
  }

  .hero__subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-6);
    max-width: 520px;
    font-family: var(--font-body);
  }

  .hero__actions {
    display: flex;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  .hero__proof {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
  }

  .hero__proof-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    font-family: var(--font-body);
  }

  .hero__proof-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 22px;
    height: 22px;
    min-width: 22px;
    border-radius: 50%;
    background-color: var(--color-accent-light);
    color: var(--color-accent);
  }

  /* Hero card */
  .hero__visual {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-2xl);
    box-shadow: var(--shadow-xl);
    padding: var(--space-8);
    max-width: 360px;
    width: 100%;
    animation: fadeInUp 0.7s ease forwards;
  }

  .hero-card__top {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    margin-bottom: var(--space-6);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--color-border-light);
  }

  .hero-card__avatar {
    width: 64px;
    height: 64px;
    min-width: 64px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary), #D4743B);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-xl);
  }

  .hero-card__name {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: var(--text-lg);
    color: var(--color-text);
    margin-bottom: var(--space-1);
  }

  .hero-card__role {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
    font-family: var(--font-body);
    margin-bottom: var(--space-2);
  }

  .hero-card__stars {
    display: flex;
    align-items: center;
    gap: 2px;
    font-size: var(--text-xs);
    font-weight: 600;
    color: var(--color-text-secondary);
    font-family: var(--font-body);
  }

  .hero-card__stars span {
    margin-left: var(--space-1);
  }

  .hero-card__skills {
    display: flex;
    gap: var(--space-2);
    flex-wrap: wrap;
    margin-bottom: var(--space-6);
  }

  .hero-card__skills span {
    font-size: var(--text-xs);
    font-weight: 500;
    font-family: var(--font-body);
    color: var(--color-primary);
    background-color: var(--color-primary-light);
    padding: var(--space-1) var(--space-3);
    border-radius: 999px;
  }

  .hero-card__footer {
    background-color: var(--color-accent-light);
    border-radius: var(--radius-lg);
    padding: var(--space-3) var(--space-4);
    text-align: center;
  }

  .hero-card__available {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    font-size: var(--text-sm);
    font-weight: 600;
    color: var(--color-accent-dark);
    font-family: var(--font-body);
  }

  .hero-card__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: var(--color-accent);
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  @media (max-width: 1024px) {
    .hero__title { font-size: var(--text-5xl); }
  }

  @media (max-width: 768px) {
    .hero__inner { grid-template-columns: 1fr; gap: var(--space-8); }
    .hero__visual { display: none; }
    .hero__title { font-size: var(--text-4xl); }
    .hero__actions { flex-direction: column; }
  }
</style>
